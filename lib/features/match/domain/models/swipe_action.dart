/// The three possible swipe interactions on a discovery card.
///
/// Values are local-only in Task 7.1.
/// Persistence is added in Tasks 7.2 (pass) and 7.3 (like / superLike).
enum SwipeAction {
  pass,
  like,
  superLike,
}
